# 5.	Explain the reasons average = x + y + z / 3
# does NOT correctly compute the average of integers x, y, z.
# Assume that x, y, and z have all been assigned integer values.
# Then write a correct expression.
# For full credit fix the program and add an explanation as a comment.


# Example:

def main():
    x = 1
    y = 2
    z = 3

    average = x + y + z / 3
    print(average)

if __name__ == "__main__":
    main()

# Write a program to determine what time it would be 7 hours
# after the current time on a twelve hour clode.on a 12 hour clock.
# Suppose it was currently 8:00 AM. The correct answer is 3. You cannot just add 8 and 7
# because that is 15.  Show how you can use an if statement to
# determine if the sum is greater than 12, and modulo (%)
# if it is, to determine the time.  Assume we are working only
# with hours (no minutes).
#
# Hint: Remember modulo % gives you the remainder from integer
# division.

def main():
    current_time = 8
    adjusted_time = 0
    time_difference = 7
    ## Put your code here

    print(adjusted_time)

if __name__ == '__main__':
    main()

